import java.util.*

fun main(args: Array<String>) {
    val sc = Scanner(System.`in`)
    val first: String = sc.next()
    val second: String = sc.next()
    println("SOMA = " + (first.toInt() + second.toInt()))
}