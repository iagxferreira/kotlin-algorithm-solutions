import java.util.*

fun main(args: Array<String>) {
    val sc = Scanner(System.`in`)
    val first: String = sc.next()
    val second: String = sc.next()
    println("PROD = " + (first.toInt() * second.toInt()))
}